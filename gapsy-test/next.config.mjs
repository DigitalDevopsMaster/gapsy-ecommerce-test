/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i5.walmartimages.com', 'placehold.co'], // Esto lo necesitamos para permitir las imagenes de este dominio
    },
  };
  
export default nextConfig;
  